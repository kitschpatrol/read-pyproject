[project]
name = "evolving-cars"
version = "0.1.0"
description = "Default template for PDM package"
authors = [{ name = "Jarek Liesen", email = "jarek.liesen@reuben.ox.ac.uk" }]
dependencies = ["jax>=0.4.38", "pygame>=2.6.1", "evosax>=0.1.6"]
requires-python = "==3.13.*"
readme = "README.md"
license = { text = "MIT" }

[tool.pdm]
distribution = false
